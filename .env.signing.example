# Copy to .env.signing (gitignored). Fill in YOUR values.
#   cp .env.signing.example .env.signing

# Name you used with: xcrun notarytool store-credentials "…"
NOTARY_PROFILE=notary-profile

# From: security find-identity -v -p codesigning
# Example: Developer ID Application: Your Name (TEAMIDHERE)
APPLE_SIGNING_IDENTITY=Developer ID Application: Your Name Or Company (TEAMIDHERE)
APPLE_TEAM_ID=TEAMIDHERE

# Only if you are NOT using a keychain profile (discouraged for local use):
# APPLE_ID=you@example.com
# APPLE_PASSWORD=xxxx-xxxx-xxxx-xxxx
